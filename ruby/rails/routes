          Rails.application.routes.draw do
#main page
            root "main#index"
# resource page crud show, new, edit, create, update, destroy
            resource :photos, only: [:show, :new]

# prefix like /admin/panels(.:format)
            namespace :admin do
              resources :panels
            end
          end
